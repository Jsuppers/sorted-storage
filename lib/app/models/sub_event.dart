/// content for a sub event
class SubEvent {
  // ignore: public_member_api_docs
  SubEvent(this.id, this.timestamp);

  /// id for this sub event
  final String id;

  /// timestamp of this sub event
  final int timestamp;
}
